{
    "name": "@nahida/extension",
    "private": true,
    "version": "3.0.1",
    "type": "module",
    "scripts": {
        "clean": "rimraf dist",
        "dev": "vite",
        "build": "tsc && pnpm run clean && vite build --config vite.content.config.ts && vite build --config vite.hook.config.ts",
        "preview": "vite preview"
    },
    "dependencies": {
        "cbor-x": "^1.6.0"
    },
    "devDependencies": {
        "@types/chrome": "^0.1.36",
        "@types/node": "^25.2.1",
        "rimraf": "^6.1.2",
        "typescript": "^5.9.3",
        "vite": "^7.3.1"
    }
}